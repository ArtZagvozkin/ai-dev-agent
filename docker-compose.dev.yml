services:
  ai-dev-agent:
    build:
      context: .
    image: artzagvozkin/ai-dev-agent:0.1.0
    volumes:
      - ./app:/app/app
      - ./logs:/app/logs
    command: >
      python -m uvicorn app.main:app
      --host 0.0.0.0
      --port 18010
      --reload
